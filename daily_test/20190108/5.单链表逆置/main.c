#include"Reverse.h"
int main() {
	//LinkList head=NULL, tail=NULL;
	//int key;
	//while (scanf("%d", &key) != EOF) {
	//	if (key == -1)break;
	//	SortInsert(&head, &tail, key);
	//}
	//printf("Output Linklist:  ");
	//print(head);
	//CutLinklist(head);
	//printf("reverse...\n");
	//reverse(&head);
	//printf("Output Linklist:  ");
	//print(head);

	//LNode a = findNode(head);
	//printf("倒数第四个节点为：%d\n", a.key);

	//LNode b = findMinNode(head);
	//printf("中间节点为：%d\n", b.key);
	LinkList head1, head2, tail1, tail2;
}